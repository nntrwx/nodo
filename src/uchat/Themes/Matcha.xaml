<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Pastel Green & Pink Color Scheme -->
    <RadialGradientBrush x:Key="WindowBackgroundBrush" 
                         GradientOrigin="0.5,0.5" Center="0.5,0.5" 
                         RadiusX="1.0" RadiusY="1.0">
        <GradientStop Color="#CDD6C4" Offset="0.0"/> <GradientStop Color="#859675" Offset="1.0"/>
    </RadialGradientBrush>
        <RadialGradientBrush x:Key="MessageListBrush"
                     GradientOrigin="1,0"
                     Center="1,0"
                     RadiusX="2" RadiusY="2">
    
    <GradientStop Color="#F2C4C4" Offset="0.0"/>
    <GradientStop Color="#FFFFFF" Offset="0.9"/>
    </RadialGradientBrush>

    <SolidColorBrush x:Key="LeftPanelBrush" Color="#C98D8D"/>
    <SolidColorBrush x:Key="ChatListBrush" Color="#D9A6A6"/>
    <LinearGradientBrush x:Key="MessageInputBrush" StartPoint="0,0" EndPoint="0,1">
    <GradientStop Color="#99E8E0D5" Offset="0.0"/>
    
    <GradientStop Color="#E8E0D5" Offset="1.0"/>
</LinearGradientBrush>
    <SolidColorBrush x:Key="AccentBrush" Color="#A5C4B8"/>
    <Color x:Key="AccentColor">#A5C4B8</Color>
    <SolidColorBrush x:Key="ForegroundPrimaryBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ForegroundSecondaryBrush" Color="#7A8A7A"/>
    <SolidColorBrush x:Key="ForegroundTetriaryBrush" Color="#3E3434"/>
    <SolidColorBrush x:Key="BorderBrushColor" Color="#C5D4B0"/>
    <Color x:Key="BorderColor">#C5D4B0</Color>
    <SolidColorBrush x:Key="RecieveBubbleBrush" Color="#E8E0D5"/>
    <SolidColorBrush x:Key="SendBubbleBrush" Color="#D9A6A6"/>  
    <SolidColorBrush x:Key="SendTimeBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="RecieveTimeBrush" Color="#857676"/>
    
    <!-- Message text colors -->
    <SolidColorBrush x:Key="ReceivedMessageTextBrush" Color="#857676"/>
    <SolidColorBrush x:Key="SentMessageTextBrush" Color="#FFF8F8"/>

    <SolidColorBrush x:Key="DeleteButtonBrush" Color="#E1B6B6" Opacity="0.60"/>
    <SolidColorBrush x:Key="DeleteTextBrush" Color="#8C5E5E"/>

    <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#7E9D85" Opacity="0.5"/>

    <Style x:Key="RoundButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="14"/>
        <Setter Property="Height" Value="14"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Color="#22000000" Opacity="0.18"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToggleIconStyle" TargetType="RadioButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="#6A8A6A"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid x:Name="GridRoot" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="GridRoot" Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#4A5A4A"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="GridRoot" Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#4A5A4A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
