<Window x:Class="uchat.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Uchat - Register" 
        Height="720" 
        Width="1280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        FontFamily="{StaticResource AppFontFamily}"
        Icon="/Assets/Images/logo.png">

    <Window.Resources>
        <!-- Background Image Brush -->
        <ImageBrush x:Key="BackgroundImage" ImageSource="/Assets/Images/background.png" Stretch="UniformToFill" TileMode="None"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#8B7355"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="20">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" 
                                                 ShadowDepth="2" 
                                                 Color="#40000000" 
                                                 Opacity="0.2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E5EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#1D1D1F"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                         Margin="{TemplateBinding Padding}"
                                         VerticalAlignment="Center"
                                         Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8B7355"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E5EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="#1D1D1F"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                         Margin="{TemplateBinding Padding}"
                                         VerticalAlignment="Center"
                                         Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8B7355"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Link TextBlock Style -->
        <Style x:Key="LinkTextBlock" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Window Control Buttons -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="#3E3434" />
            <Setter Property="Opacity" Value="0.1" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#000000" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundImage}" MouseLeftButtonDown="Header_MouseLeftButtonDown">
        <!-- Window Controls (Top Right) -->
        <StackPanel Orientation="Horizontal" 
                  HorizontalAlignment="Right" 
                  VerticalAlignment="Top"
                  Margin="10,10,10,0">
            <Button x:Name="MinimizeButton" 
                  Click="MinimizeButton_Click" 
                  Style="{StaticResource WindowControlButton}">
                <TextBlock Text="─" 
                         FontSize="16" 
                         Foreground="White"/>
            </Button>
            <Button x:Name="CloseButton" 
                  Click="CloseButton_Click" 
                  Style="{StaticResource WindowControlButton}">
                <TextBlock Text="✕" 
                         FontSize="16" 
                         Foreground="White"/>
            </Button>
        </StackPanel>

        <!-- Form Content (Right Side) -->
        <Grid HorizontalAlignment="Right" 
              VerticalAlignment="Center" 
              Width="600" 
              Margin="0,0,100,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0">
                <StackPanel VerticalAlignment="Center" 
                          HorizontalAlignment="Center" 
                          Width="400">
                    
                    <!-- Welcome Text -->
                    <TextBlock Text="New Here?" 
                             FontSize="48" 
                             FontWeight="Bold" 
                             Foreground="#3D3528" 
                             Margin="0,0,0,15"
                             HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Color="#F5F5DC" Opacity="0.4"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    
                    <TextBlock Text="Create your account to start." 
                             FontSize="18" 
                             Foreground="#5A5245" 
                             Margin="0,0,0,40"
                             HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="#F5F5DC" Opacity="0.3"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <!-- Name Field -->
                    <StackPanel Margin="0,0,0,20" HorizontalAlignment="Center" Width="350">
                        <TextBlock Text="Name" 
                                 Foreground="#4A4235" 
                                 FontSize="16" 
                                 FontWeight="Medium" 
                                 Margin="0,0,0,10">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="#F5F5DC" Opacity="0.3"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBox x:Name="NameBox" 
                               Style="{StaticResource ModernTextBox}" 
                               KeyDown="HandleEnterKey"/>
                    </StackPanel>

                    <!-- Username Field -->
                    <StackPanel Margin="0,0,0,20" HorizontalAlignment="Center" Width="350">
                        <TextBlock Text="Username" 
                                 Foreground="#4A4235" 
                                 FontSize="16" 
                                 FontWeight="Medium" 
                                 Margin="0,0,0,10">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="#F5F5DC" Opacity="0.3"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBox x:Name="UsernameBox" 
                               Style="{StaticResource ModernTextBox}" 
                               KeyDown="HandleEnterKey"/>
                    </StackPanel>

                    <!-- Password Field -->
                    <StackPanel Margin="0,0,0,30" HorizontalAlignment="Center" Width="350">
                        <TextBlock Text="Password" 
                                 Foreground="#4A4235" 
                                 FontSize="16" 
                                 FontWeight="Medium" 
                                 Margin="0,0,0,10">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="#F5F5DC" Opacity="0.3"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <PasswordBox x:Name="PasswordBox" 
                                   Style="{StaticResource ModernPasswordBox}" 
                                   KeyDown="HandleEnterKey"/>
                    </StackPanel>

                    <!-- Create Account Button -->
                    <Button x:Name="RegisterButton" 
                          Content="Create Account" 
                          Click="RegisterButton_Click" 
                          Style="{StaticResource ModernButton}" 
                          Margin="0,0,0,20"
                          HorizontalAlignment="Center"
                          Width="350"/>

                    <!-- Sign In Link -->
                    <StackPanel Orientation="Horizontal" 
                              HorizontalAlignment="Center">
                        <TextBlock Text="Already have an account? " 
                                 Foreground="#5A5245" 
                                 FontSize="15" 
                                 VerticalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="#F5F5DC" Opacity="0.3"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock x:Name="SignInLink" 
                                 Text="Sign in" 
                                 Style="{StaticResource LinkTextBlock}" 
                                 Foreground="#3D3528"
                                 FontSize="15"
                                 MouseLeftButtonDown="SignInLink_MouseLeftButtonDown">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="#F5F5DC" Opacity="0.3"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>

                    <!-- Error Message -->
                    <TextBlock x:Name="ErrorText" 
                             Text="" 
                             Foreground="#FF3B30" 
                             FontSize="14" 
                             TextAlignment="Center" 
                             Margin="0,20,0,0" 
                             Visibility="Collapsed">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="3" ShadowDepth="1" Color="White" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
