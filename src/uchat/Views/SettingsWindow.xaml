<Window x:Class="uchat.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:uchat.Converters"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        Title="Uchat - Settings" 
        Height="700" 
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:StringToBoolConverter x:Key="StringToBoolConverter"/>

        <Style x:Key="SettingsButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="32">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource SettingsButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource DeleteButtonBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource DeleteTextBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{DynamicResource WindowBackgroundBrush}" MouseLeftButtonDown="Header_MouseLeftButtonDown">
        
        <Border Background="{DynamicResource LeftPanelBrush}" Margin="10" Padding="40" CornerRadius="32" BorderThickness="1" BorderBrush="{DynamicResource BorderBrushColor}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Color="Black" Opacity="0.3"/>
            </Border.Effect>
            
            <ScrollViewer Style="{StaticResource ModernScrollViewer}" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Profile Settings"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ForegroundPrimaryBrush}"
                               Margin="0,0,0,30"/>

                    <Border Background="{DynamicResource ChatListBrush}" Padding="25" Margin="0,0,0,20" CornerRadius="32">
                        <StackPanel>
                            <TextBlock Text="Profile"
                                       FontSize="20"
                                       Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                       FontWeight="Bold"
                                       Margin="0,0,0,20"/>

                            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                                <Button x:Name="AvatarButton"
                                        Width="100"
                                        Height="100"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="AvatarButton_Click">
                                    <Grid>
                                        <Ellipse x:Name="AvatarCircle" 
                                                 Width="100" 
                                                 Height="100"
                                                 Stroke="{DynamicResource AccentBrush}"
                                                 StrokeThickness="2">
                                            <Ellipse.Fill>
                                                <ImageBrush x:Name="AvatarImage" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <Border Background="#80000000" 
                                                Width="100" 
                                                Height="30"
                                                VerticalAlignment="Bottom">
                                            <TextBlock Text="Change" 
                                                       Foreground="White" 
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </Button>
                                <TextBlock Text="Click to change avatar" 
                                           FontSize="12" 
                                           Foreground="{DynamicResource ForegroundSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,5,0,0"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Name"
                                          Foreground="{DynamicResource ForegroundSecondaryBrush}"
                                          FontSize="14"
                                          FontWeight="Medium"
                                          Margin="0,0,0,5"/>
                                <TextBox x:Name="DisplayNameBox"
                                         FontSize="16"
                                         Padding="12"
                                         Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrushColor}"
                                         BorderThickness="1"
                                         Background="{DynamicResource MessageInputBrush}"/>
                            </StackPanel>


                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="Profile info"
                                          Foreground="{DynamicResource ForegroundSecondaryBrush}"
                                          FontSize="14"
                                          FontWeight="Medium"
                                          Margin="0,0,0,5"/>
                                <TextBox x:Name="ProfileInfoBox"
                                         FontSize="14"
                                         Padding="12"
                                         Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrushColor}"
                                         BorderThickness="1"
                                         Background="{DynamicResource MessageInputBrush}"
                                         Height="60"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"/>
                            </StackPanel>

                            <Button x:Name="SaveButton"
                                    Content="Save changes"
                                    Style="{StaticResource SettingsButtonStyle}"
                                    HorizontalAlignment="Right"
                                    Width="200"
                                    Click="SaveButton_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource ChatListBrush}" Padding="25" Margin="0,0,0,20" CornerRadius="32">
                        <StackPanel>
                            <TextBlock Text="Theme"
                                       FontSize="20"
                                       Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                       FontWeight="Bold"
                                       Margin="0,0,0,15"/>

                            <StackPanel HorizontalAlignment="Center">
                                <RadioButton x:Name="LatteThemeRadio"
                                             Content="Latte"
                                             GroupName="Theme"
                                             Margin="0,10,0,0"
                                             FontSize="14"
                                             Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                             Checked="ThemeRadioButton_Checked"/>

                                <RadioButton x:Name="MatchaThemeRadio"
                                             Content="Matcha"
                                             GroupName="Theme"
                                             Margin="0,10,0,0"
                                             FontSize="14"
                                             Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                             Checked="ThemeRadioButton_Checked"/>

                                <RadioButton x:Name="AcaiThemeRadio"
                                             Content="Acai"
                                             GroupName="Theme"
                                             Margin="0,10,0,0"
                                             FontSize="14"
                                             Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                             Checked="ThemeRadioButton_Checked"/>

                                <RadioButton x:Name="EarlGreyThemeRadio"
                                             Content="EarlGrey"
                                             GroupName="Theme"
                                             Margin="0,10,0,0"
                                             FontSize="14"
                                             Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                             Checked="ThemeRadioButton_Checked"/>

                                <RadioButton x:Name="MulledWineThemeRadio"
                                             Content="MulledWine"
                                             GroupName="Theme"
                                             Margin="0,10,0,0"
                                             FontSize="14"
                                             Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                             Checked="ThemeRadioButton_Checked"/>

                                <RadioButton x:Name="AnchanThemeRadio"
                                             Content="Anchan"
                                             GroupName="Theme"
                                             Margin="0,10,0,0"
                                             FontSize="14"
                                             Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                             Checked="ThemeRadioButton_Checked"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <StackPanel Margin="0,20,0,0">
                        <Button x:Name="DeleteAccountButton"
                                Content="Delete account"
                                Style="{StaticResource DangerButtonStyle}"
                                Height="45"
                                Click="DeleteAccountButton_Click"/>
                    </StackPanel>

                    <Button x:Name="CloseButton"
                            Content="Close"
                            Style="{StaticResource SettingsButtonStyle}"
                            Background="Transparent"
                            Foreground="{DynamicResource ForegroundSecondaryBrush}"
                            BorderBrush="{DynamicResource BorderBrushColor}"
                            BorderThickness="1"
                            Margin="0,30,0,0"
                            HorizontalAlignment="Center"
                            Width="100"
                            Click="CloseButton_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0">
            <Button Style="{DynamicResource RoundButtonStyle}"
                    Background="#FF5F56"
                    Click="WindowCloseButton_Click">
                <Path Data="M192,64.34L179.66,52,128,103.66,76.34,52,64,64.34,115.66,116,64,167.66,76.34,180,128,128.34,179.66,180,192,167.66,140.34,116Z"
                   Fill="Black" Stretch="Uniform" Width="8" Height="8"/>
            </Button>

            <Button Style="{DynamicResource RoundButtonStyle}"
                    Background="#FFBD2E" 
                    Click="WindowMinimizeButton_Click"
                    Margin="5,0,0,0">
                 <Path Data="M216,128a8,8,0,0,1-8,8H48a8,8,0,0,1,0-16H208A8,8,0,0,1,216,128Z"
                      Fill="Black" Stretch="Uniform" Width="8" Height="8"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>